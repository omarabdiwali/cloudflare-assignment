export const metadata = {
  title: 'Format CSV',
  description: 'Format CSV To JSON',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}